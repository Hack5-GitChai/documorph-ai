fastapi
uvicorn[standard]
python-multipart
python-docx

# For Hugging Face TrOCR & general ML
transformers[sentencepiece] # Includes sentencepiece for tokenizers
torch --index-url https://download.pytorch.org/whl/cpu # Specify CPU version of PyTorch for broader compatibility in Codespaces
Pillow # For image manipulation
# numpy # Often a dependency, pip will handle it
# easyocr # We are replacing this with TrOCR for now. Remove if you only want TrOCR.